// MUI imports
import { DateCalendar } from '@mui/x-date-pickers/DateCalendar';

function CalTest() {
    return (
        <>
            <h2>Calendar Component</h2>
            <p>This page is now defunct, but I will need a date picker for the edit function, so this will be held here until implemented elsewhere.</p>
            <DateCalendar />
        </>
    )
}

export default CalTest;